import { Input } from './components/ui/Input';

export const App = () => {
  return (
    <div style={{ margin: 100 }}>
      <Input label="Input" placeholder="Input" type="search" />
    </div>
  );
};
